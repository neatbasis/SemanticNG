## Summary

- Describe what changed and why.

## Validation

- [ ] I ran the required tests for this change.
- [ ] If this PR touches `src/state_renormalization/`, I ran all mandatory in-progress milestone commands from `docs/dod_manifest.json`.
- [ ] For every capability status transition in `docs/dod_manifest.json`, this PR description includes the exact milestone `pytest` command(s) from the manifest and linked passing evidence for each command (CI run URL, job URL, or command output snippet).

## Capability updates (if applicable)

- Capability ID(s):
- Status transition(s):
- Exact milestone pytest command(s):
- Linked passing test evidence for each command:
