## Summary

- Describe what changed and why.

## Validation

- [ ] I ran the required tests for this change.
- [ ] If this PR touches `src/state_renormalization/`, I ran all mandatory in-progress milestone commands from `docs/dod_manifest.json`.
- [ ] For every capability status transition in `docs/dod_manifest.json`, this PR description includes the exact milestone `pytest` command(s) from the manifest.
- [ ] For every capability status transition in `docs/dod_manifest.json`, this PR description links passing CI evidence for each listed command (workflow URL + job/step URL, or attached command output).

## Capability updates (if applicable)

- Capability ID(s):
- Status transition(s):
- Exact milestone pytest command(s):
- Linked passing CI evidence for each command:
